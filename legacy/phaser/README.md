# Legacy Phaser Prototype

This folder contains an older Phaser-based prototype that is not used by the current Three.js + Vite build.

- Active game entrypoint: `src/main.js`
- Phaser files were moved out of `src/` to prevent confusion and keep the production codepath single-stack.

